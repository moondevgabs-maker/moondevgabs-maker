name: Generate Snake Animation
on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

# Permissão para o robô escrever no repositório
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Generate Snake SVG
        uses: Platane/snk@v3
        with:
          # O seu nome de utilizador do GitHub
          github_user_name: ${{ github.repository_owner }}
          
          # ----- CÓDIGO DE CORES CORRIGIDO (com %23) -----
          outputs: |
            dist/snake.svg?color_snake=%23BD93F9,%23FF79C6,%238BE9FD&color_dots=%2344475A&color_bg=%23282A36
            dist/snake-light.svg?palette=github-light

      - name: Commit & Push
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "chore: update contribution snake (fix colors)"
          file_pattern: dist/*.svg
